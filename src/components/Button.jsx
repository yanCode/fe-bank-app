const Button = ({styles}) => {
  return (
    <button type='button'
            className={`bg-blue-gradient py-4 px-6 font-poppins text-primary text-[18px] outline-none rounded-[10px] ${styles}`}>Get Started</button>)
}

export default Button